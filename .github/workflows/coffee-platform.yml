name: Coffee Platform CI/CD

run-name: Coffee Platform · ${{ github.ref_name }} · ${{ github.sha }}

on:
  push:
    branches:
      - main
      - "phase5/**"
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    name: Quality gate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    concurrency:
      group: quality-${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Use the verified Node.js runtime
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.19.0
          cache: npm

      - name: Install locked dependencies
        run: npm ci --include=dev

      - name: Verify CSS build toolchain
        run: node -e "require.resolve('@tailwindcss/postcss'); require.resolve('tailwindcss')"

      - name: Lint
        run: npm run lint

      - name: Test and build
        run: npm test

      - name: Reject generated source changes
        run: git diff --exit-code

      - name: Upload auditable Next.js build artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: coffee-platform-next-${{ github.sha }}
          path: |
            .next/**
            !.next/cache/**
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 7
          compression-level: 9
